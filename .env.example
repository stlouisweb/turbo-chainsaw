# Database (matches docker-compose defaults)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=apppassword

# API server
API_PORT=3000
API_HOST=0.0.0.0
NODE_ENV=development

# CORS origin (Vite default)
CORS_ORIGIN=http://localhost:5173
